import math

def main():

    dim = int(input("Inserire la dimensione della matrice: "))
    lista = []
    lunghRiga = math.sqrt(dim)
    boolPunto = False

    for k in range(dim):
        num = int(input(f"Inserire il {k} numero: "))
        lista.append(num)

    for el in range(len(lista)):
        for k in range(lunghRiga):
            for j in range():
            if lista[el] >= lista[lunghRiga]

if __name__ == "__main__":
    main()